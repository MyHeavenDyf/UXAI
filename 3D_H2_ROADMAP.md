# 3D Agent 功能 · 工作规划

> 目标：用户用自然语言生成 3D 场景，生成后可预览、编辑、调参、导出工程。
> **整体完成时间：11 月 30 日。**

## 一、五大能力块

| # | 能力块 | 做什么 | 完成事件（达标定义） | 完成 |
|---|---|---|---|---|
| 1 | **构建 3D 组件库** | 沉淀可复用的 3D 场景组件（网格、墙体、热力、标注等），配套文档，供生成时直接调用 | 组件齐全 + 文档可被 AI 检索调用 | 9 月底 |
| 2 | **构建 3D 资源库** | 模型/贴图资产库，支持检索与 AI 按需生成缺失模型（含兜底） | 资产可检索 + AI 按需生成 + 失败有兜底 | 9 月底 |
| 3 | **接入 3D 设计规范** | 把组件能力、设计规范文档喂给 AI，指导其生成符合规范的场景 | 规范文档接入 AI，生成结果符合规范 | 10 月中 |
| 4 | **构建 3D 场景生成能力** | 自然语言 → 自动生成 3D 场景，含「生成—评审—精修」自动闭环 | 一句话生成场景 + 自动评审精修达标出图 | 10 月底 |
| 5 | **构建编辑调参能力** | 生成后可预览、编辑、调参，并导出工程供二次开发 | 生成后可编辑/调参/导出工程，可二次开发 | 11 月底 |

## 二、架构图

```
              自然语言需求
                  │
                  ▼
        ┌──────────────────┐
        │  3D 场景生成引擎   │ ◄──── 3D 设计规范（指导生成）
        │     (AI Agent)    │ ◄──── 3D 组件库（调用组件）
        └────────┬─────────┘ ◄──── 3D 资源库（调用模型/生成）
                 │ 生成的 3D 场景
                 ▼
        ┌──────────────────┐
        │  预览 / 编辑 / 调参 / 导出工程 │
        └──────────────────┘
```

**一句话流程**：用户描述 → AI 基于组件库/资源库/设计规范生成 3D 场景 → 生成后预览、编辑、调参、导出。

## 三、时间线

```
 9月            10月           11月
  │              │              │
  ├─ 组件库 ─┐    │              │
  ├─ 资源库 ─┤    ├─ 设计规范 ─┐  │
  │         │    ├─ 生成能力 ─┤  ├─ 编辑调参 ─┐
  │         │    │           │  │           │
  ▼         ▼    ▼           ▼  ▼           ▼
 底座就绪 ────▶ 生成闭环 ────▶ 编辑导出 ──▶ 11/30 整体完成
```

- **9 月**：组件库 + 资源库（底座）
- **10 月**：设计规范 + 生成能力（生成闭环跑通）
- **11 月**：编辑调参 + 导出（整体交付）

## 四、当前进展

五大能力底座代码已基本就绪，当前进入**联调与真实 AI 接入**阶段，按上述节奏推进至 11/30 整体完成。
